services:
  - type: web
    name: berlin-events-finder
    env: python
    buildCommand: |
      pip install -r requirements.txt
      cd frontend
      yarn install --frozen-lockfile
      yarn build:prod
    startCommand: |
      cd frontend
      yarn preview --host 0.0.0.0 --port $PORT
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_OPTIONS
        value: --max-old-space-size=512
      - key: PORT
        value: 10000
    healthCheckPath: /
    autoDeploy: true
    plan: starter 